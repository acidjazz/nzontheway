.close
.info
  .right
    .username #{username}
    .duration #{duration} ago
  .profile
    img('src'=profile)
  .clear
  .caption #{caption}
  .share(data-id=id,data-image=image) share
    .share_f
  .totals
    | #{likes}
    .sprite.likes('title'= likes + ' likes')
    | #{tags}
    .sprite.tags('title'= tags + ' tags')
    | #{comments}
    .sprite.comments('title'= comments + ' comments')
.image
  img('src'=image)
